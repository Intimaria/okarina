#pragma once
// Copiá este archivo a secrets.h y completá los valores.
// secrets.h está gitignored — nunca lo commitees.
// Son las MISMAS credenciales que los otros nodos (misma red, mismo broker);
// el DEVICE_ID va en node-power.ino (power-01, power-02, ...).


#define WIFI_SSID  "tu-wifi"
#define WIFI_PASS  "tu-password-wifi"

#define MQTT_HOST  "mqtt.example.com"
#define MQTT_PORT  443
#define MQTT_USER  "nodos"
#define MQTT_PASS  "la-password-mqtt"
